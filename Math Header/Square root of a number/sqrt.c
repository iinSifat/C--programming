#include <stdio.h>
#include <math.h>
int main ()
{
            double a,result;
            printf ("enter the number:");
            scanf("%lf",&a);
            result= sqrt(a);
            printf ("square root of %.2lf =%.2lf",a,result);

            return 0;

}
