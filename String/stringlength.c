#include<stdio.h>
#include<string.h>
int main()
{
    char A[100];
    char B[50];
    gets(A);
    gets(B);
    printf("Length of the String is %d\n",strlen(A));
        printf("Length of the String is %zu",strlen(B));//sonet sir pdf zu

}